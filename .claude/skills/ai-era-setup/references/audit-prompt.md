# 审计子代理 prompt（填空后原文发给一个只读子代理）

你是一个只读评估员。任务：判断项目 `<REPO_PATH>` 在多大程度上满足 `<STANDARD_PATH>`（AI 时代软件状态机测试与可观测性标准），产出逐条对照的符合度报告。不要修改任何文件。

先完整读标准，再看项目。标准写于另一个项目：区分「原则」（状态契约、oracle、不变量自动检查、打点、trace 贯穿、语义穷举）与「原项目特有条目」（工作空间路径、定时任务、Electron 主题）——后者只判「有无对应物」，不机械判不满足。

项目入口：`AGENTS.md` / `README.md` / 状态与实现表 / 测试报告 / 测试目录 / 服务端与前端主模块 / 证据目录（按仓库实际填）。

工具纪律：查符号、调用链、影响面用 codegraph（英文符号名）；已知固定字面量在哪个文件可用 grep；长输出用沙箱执行只打印结论。

报告要求：每条判断带 **file:line 证据**；没证据写「未找到」。等级：满足（代码 + 测试/证据）/ 部分（有代码没测试，或断言不在承重面：只断类名/隐藏元素/中间函数）/ 不满足 / 不适用（写理由）。

输出：
1. 总评 3–5 句：处于什么水平，最大的一到两个缺口。
2. 对照表：标准每个主要章节（状态契约 / 语义穷举+oracle / 组合爆炸 / UI 元素穷举 / 全链路打点 / 不变量 / 主题布局 / 架构 / 验收标准 / 两层状态机）→ 等级 + 证据 + 一句话。
3. 「测试是否落在承重面」抽查三条：断言打在用户可见契约（文案/接口/盘上文件）还是内部结构。
4. 按投入产出排的前五条行动，每条一行。
5. 你的证据边界：没看什么、什么是推断。

报告直接作为最终回复返回，不写文件。
